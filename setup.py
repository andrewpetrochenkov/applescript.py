from setuptools import setup

setup(
    name='applescript',
    version='2020.5.31',
    install_requires=[
        'only',
        'public',
        'setuptools',
    ],
    packages=[
        'applescript',
    ],
)
