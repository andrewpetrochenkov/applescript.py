from setuptools import setup

setup(
    name='applescript',
    version='2020.4.15',
    install_requires=[
        'only',
        'public',
        'setuptools',
    ],
    packages=[
        'applescript',
    ],
)
