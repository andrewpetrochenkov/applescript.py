from setuptools import setup

setup(
    name='applescript',
    version='2020.6.14',
    install_requires=[
        'only',
        'setuptools',
    ],
    packages=[
        'applescript',
    ],
)
